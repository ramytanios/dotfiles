if status is-interactive
    # Commands to run in interactive sessions can go here
end

fish_vi_key_bindings

alias vi "nvim"

alias gs "git status"
alias ga "git add"
alias gc "git commit"
alias gp "git push"
